## git이란?

1. 버전 관리 프로그램
2. 터미널에서 git --version

---

## 초기 설치시 git 세팅

1. 터미널에 아래 코드를 순서대로 작성
git config --global user.name '이름'
git config -- global user.emai '이름@gmail.com'

---

## github 코딩파일 첫 업로드

1. 내 프로필 repositories 클릭
2. New 클릭
3. Repository name에는 코딩파일 제목 입력
4. Create Repository 클릭

5. VS code 좌측패널에서 git 아이콘 클릭
6. Initialize Repository 클릭
7. 업로드 할 파일만 + 눌러서 Staged Changes로 이동
8. Message 입력칸에 어떤 것이 변경되었는지 입력하고 Commit 버튼 클릭

9. 터미널에 순차적으로 복붙
git remote add origin https://github.com/s0eun312/git-study.git
git push -u origin main

---

## github에 코딩파일 수정 후 업로드

1. 업로드 할 파일만 + 눌러서 Staged Changes로 이동
2. Message 입력칸에 어떤 것이 변경되었는지 입력하고 Commit 버튼 클릭
3. [sync Changes 위화살표] 버튼 클릭하면 내 컴퓨터에서 github으로 코드 전송